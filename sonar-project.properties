# SonarCloud (CI-based analysis) — projectKey/organization must match SonarCloud project
sonar.projectKey=mohdashraf010897_auto-sonar-poc
sonar.organization=mohdashraf010897

# Sources and exclusions
sonar.sources=src
sonar.exclusions=**/node_modules/**,**/dist/**,**/*.test.ts,**/*.test.tsx
sonar.sourceEncoding=UTF-8

# Optional: test coverage (if you add coverage later)
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
