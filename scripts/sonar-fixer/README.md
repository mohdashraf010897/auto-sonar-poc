# Sonar fixer (reset)

Fixer scripts have been removed. The repo uses **report only**: the workflow fetches SonarCloud issues and opens a PR with the issue list.

To add an autonomous auto-fixer pipeline, see **docs/AUTO-SONAR-FIXER-PLAN.md**.
